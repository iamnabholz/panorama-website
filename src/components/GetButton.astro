---
import { Image } from "astro:assets";
import chrome from "../assets/chrome.svg";
import firefox from "../assets/firefox.svg";
import edge from "../assets/edge.svg";

const browsers = [
    {
        name: "Chrome",
        icon: chrome,
        href: "https://chrome.google.com/webstore/detail/panorama-tab/einjlbiknhpkccefknpcnmnmmnhigcbc",
    },
    {
        name: "Firefox",
        icon: firefox,
        href: "https://addons.mozilla.org/addon/panorama-tab/",
    },
    {
        name: "Edge",
        icon: edge,
        href: "https://microsoftedge.microsoft.com/addons/detail/panorama-tab/pfpebaokeeiinfhcklkomfbdejpfaoke",
    },
];
---

<section>
    <p class="muted">Available on</p>
    <div class="row">
        {
            browsers.map(({ name, icon, href }) => (
                <a href={href}>
                    <Image
                        alt={`${name} Browser Logo`}
                        src={icon}
                        width={44}
                        height={44}
                        loading="eager"
                    />
                </a>
            ))
        }
    </div>
</section>

<style>
    img {
        height: 44px;
        width: 44px;
        filter: drop-shadow(0 0 10px rgba(0, 0, 0, 0.2));
    }

    .row {
        gap: 6px;
    }

    .muted {
        font-size: 1rem;
        margin-bottom: 8px;
    }
</style>
